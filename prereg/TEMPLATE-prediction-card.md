# Prediction Card — <Stage/Exp ID> : <short title>

`[prereg tag: prereg-XX — NOT YET TAGGED until predictions finalized + advisor ack]`

- **Committed:** `<YYYY-MM-DD on tag>`  **Advisor ack:** `<date | pending>`
- **Maps to:** `<Hn / wedge Wn / RQn>`

## Hypothesis (falsifiable, one sentence)
<...>

## Mechanism (why we expect it)
<...>

## Quantitative prediction (direction + threshold + your prior confidence %)
<e.g. "metric X ≥ Y under condition Z; prior ~NN%">

## Measurement protocol (exact)
- Box / build (pinned hashes) / model (file hash) / quant / seeds / N≥5 runs
- Procedure: <...>

## Analysis plan
- metric formula; report P50 & P99; CI method; baseline + measured noise floor

## Pass gate
<proceed if ...>

## Kill / pivot gate
<stop or pivot to <X> if ...>

## Pre-committed pivot (decided NOW, not after results)
<...>
