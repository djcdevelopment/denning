# Results — <Stage/Exp ID> : <short title>

**References pre-registration:** `prereg-XX` (commit `<hash>`, tagged `<date>`).
**This file committed:** `<date>` (AFTER the prereg tag — the ordering is the integrity proof).

## Predicted vs actual (the honest unveiling)

| Quantity | Predicted (from prereg) | Actual (measured) | Within prediction? |
|----------|-------------------------|-------------------|--------------------|
| <metric> | <value + prior %> | <value ± CI> | yes / no / partial |

## What we did
- Manifest: see `REPRODUCE.md` entry `<id>` (commit, build hashes, model hash, seeds, configs).
- Deviations from the protocol (and why): <...>

## What we found
- Plots / tables / stats (P50 & P99, CIs, noise floor).
- Honest discussion of every deviation from prediction — no post-hoc edits to the prereg.

## Gate decision
- Per the pre-committed pass/kill gate: **PROCEED / PIVOT / STOP** — reasoning.
- Advisor packet: `<link>`. Advisor decision + date: `<...>`.
